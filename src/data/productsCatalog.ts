import { ProductItem } from '@/types/product';

// Empty default catalog — all products are dynamically fetched from the database / admin portal
export const initialProductsCatalog: ProductItem[] = [];
